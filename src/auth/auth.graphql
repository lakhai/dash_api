type Auth {
	expiresIn: String!
	accessToken: String!
}

type Mutation {
	login(email: String!, password: String!): Auth
	register(email: String!, firstName: String!, lastName: String!, password: String!): Auth
}